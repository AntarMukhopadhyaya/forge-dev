import { createClient } from "@supabase/supabase-js"; const supabaseUrl =
{{envAccessor}}.{{urlEnv}}; const supabaseKey =
{{envAccessor}}.{{keyEnv}}; if (!supabaseUrl || !supabaseKey) { throw new Error("Missing
Supabase environment variables."); } export const supabase = createClient(supabaseUrl,
supabaseKey);